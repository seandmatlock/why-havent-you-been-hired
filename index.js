let titleEl = document.getElementById("title-text")
let buttonEl = document.getElementById("message-btn")
let messageEl = document.getElementById("message")

function message() {
    titleEl.textContent = "You don't?"
    buttonEl.textContent = "Hold up, I haven't figured out switch cases yet, so I'll update once I do" 
}

